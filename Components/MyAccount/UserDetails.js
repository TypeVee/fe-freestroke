import { StyleSheet, Text , View } from 'react-native';

export default function UserDetails() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>This is where user details are!</Text>
      </View>
    )
  }
